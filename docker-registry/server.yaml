name: arr
image: ghcr.io/davidgibbons/mcp-arr
type: server
meta:
  category: media
  tags:
    - sonarr
    - radarr
    - lidarr
    - prowlarr
    - whisparr
    - chaptarr
    - media-management
    - plex
    - homelab
    - entertainment
about:
  title: "*arr Media Management"
  description: "MCP server for the *arr media management suite - Sonarr (TV), Radarr (Movies), Lidarr (Music), Prowlarr (Indexers), Whisparr (Adult), and Chaptarr (Books). Manage your media library, search for content, monitor downloads, and get AI-powered configuration recommendations using TRaSH Guides."
  icon: https://wiki.servarr.com/assets/servarr.png
source:
  project: https://github.com/davidgibbons/mcp-arr
  commit: 50131f2b3cdb9f0ce4339dbaa6e4303479ce1926
config:
  description: |
    Configure URLs and API keys for your *arr services. Only configure the services you use -
    the server automatically detects which are available. Find API keys in each application
    under Settings > General > Security.
  env:
    - name: SONARR_URL
      description: "Sonarr base URL (e.g., http://localhost:8989 or http://sonarr:8989)"
      example: "http://localhost:8989"
    - name: RADARR_URL
      description: "Radarr base URL (e.g., http://localhost:7878 or http://radarr:7878)"
      example: "http://localhost:7878"
    - name: LIDARR_URL
      description: "Lidarr base URL (e.g., http://localhost:8686 or http://lidarr:8686)"
      example: "http://localhost:8686"
    - name: PROWLARR_URL
      description: "Prowlarr base URL (e.g., http://localhost:9696 or http://prowlarr:9696)"
      example: "http://localhost:9696"
    - name: WHISPARR_URL
      description: "Whisparr base URL (e.g., http://localhost:6969 or http://whisparr:6969)"
      example: "http://localhost:6969"
    - name: CHAPTARR_URL
      description: "Chaptarr base URL (e.g., http://localhost:8789 or http://chaptarr:8789)"
      example: "http://localhost:8789"
  secrets:
    - name: arr.sonarr-api-key
      env: SONARR_API_KEY
      description: "Sonarr API key from Settings > General > Security"
      example: "<your-sonarr-api-key>"
    - name: arr.radarr-api-key
      env: RADARR_API_KEY
      description: "Radarr API key from Settings > General > Security"
      example: "<your-radarr-api-key>"
    - name: arr.lidarr-api-key
      env: LIDARR_API_KEY
      description: "Lidarr API key from Settings > General > Security"
      example: "<your-lidarr-api-key>"
    - name: arr.prowlarr-api-key
      env: PROWLARR_API_KEY
      description: "Prowlarr API key from Settings > General > Security"
      example: "<your-prowlarr-api-key>"
    - name: arr.whisparr-api-key
      env: WHISPARR_API_KEY
      description: "Whisparr API key from Settings > General > Security"
      example: "<your-whisparr-api-key>"
    - name: arr.chaptarr-api-key
      env: CHAPTARR_API_KEY
      description: "Chaptarr API key from Settings > General > Security"
      example: "<your-chaptarr-api-key>"
